# MongoDB Atlas connection URI (preferred) — get one at https://www.mongodb.com/cloud/atlas (free tier)
# Example: mongodb+srv://user:pass@cluster0.xxxxx.mongodb.net/brazilian-addon-db?retryWrites=true&w=majority
MONGODB_URI=

# Legacy individual variables (used only when MONGODB_URI is not set)
DB_HOST=localhost
DB_PORT=27017
DB_NAME=brazilian-addon-db
DB_USER=
DB_PSK=

# How often (ms) to refresh stale seeders on a stream GET (default 30 days).
SEEDERS_REFRESH_MS=2592000000

# Upstream addon(s) for stream scraping. Comma-separated list — first responder wins.
# Recommended: deploy the Cloudflare Worker in cloudflare-worker/ and point this here
# to bypass Torrentio's cloud-IP block.
TORRENTIO_BASE=https://torrentio.strem.fun

# Shared secret matching the Cloudflare Worker's WORKER_SECRET (optional).
WORKER_SECRET=

# Debrid backend. 'direct' (default) calls Real-Debrid and TorBox APIs directly,
# with our delete-on-fail cleanup to avoid orphan torrents on the user's account.
# 'stremthru' is opt-in: requires a paid/self-hosted StremThru instance with
# STREMTHRU_AUTH credentials — the public elfhosted endpoint is gated and
# returns 403 without auth.
DEBRID_BACKEND=direct

# StremThru base URL (only used when DEBRID_BACKEND=stremthru).
STREMTHRU_BASE_URL=https://stremthru.elfhosted.com

# Basic auth for self-hosted/paid StremThru instances.
STREMTHRU_AUTH=

# BR torrent indexer. Default is the public community instance.
TORRENT_INDEXER_BASE=https://torrent-indexer.darklyn.org

# Knaben meta-indexer (50+ trackers including Nyaa for anime dublado).
KNABEN_BASE=https://api.knaben.org/v1

# Nyaa.si direct RSS for anime dublado PT-BR.
NYAA_BASE=https://nyaa.si

# ThePirataFilmes API mirrors (comma-separated).
PIRATAFILMES_BASE=https://www.thepiratafilmes.online,https://catalago.online

# Other Stremio addons we consume as upstream sources. Comma-separated,
# each entry can be `name|url` for a stable label or just `url`. Default
# includes guindex-stremio. Set to empty string to disable.
STREMIO_ADDON_SOURCES=guindex|https://guindex-stremio.vercel.app

# Circuit breaker tuning (per provider). After N failures the provider is
# short-circuited for COOLDOWN_MS to avoid wasting timeout budget.
BREAKER_FAILURE_THRESHOLD=2
BREAKER_COOLDOWN_MS=900000

# Short-lived response cache TTL in ms. Default 30 minutes.
SEARCH_CACHE_TTL_MS=1800000

# Vercel Cron auth secret. Generate any random string and add it on Vercel
# (Settings → Environment Variables → CRON_SECRET).
CRON_SECRET=

# TMDB API key for PT-BR title/description enrichment (optional).
# Get one at https://www.themoviedb.org/settings/api
TMDB_API_KEY=
